import { describe, it, expect } from 'vitest';
import VueNiceActionDropdown from '../src/VueNiceActionDropdown.vue';

describe('VueNiceActionDropdown', () => {
  it('is defined', () => {
    expect(VueNiceActionDropdown).toBeDefined();
  });
});
